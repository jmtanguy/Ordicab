# Store Contract

- IPC calls live in Zustand store actions, never in React components.
- All stores use Zustand with Immer middleware for consistent mutation semantics.
- Story 1.2 stores are foundational scaffolds only; feature workflows are implemented in later stories.
